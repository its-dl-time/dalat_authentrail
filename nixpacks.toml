[phases.install]
cmds = ["npm install -g pnpm@9 && pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @dalat/types build && pnpm --filter @dalat/api build"]

[start]
cmd = "node apps/api/dist/index.js"
